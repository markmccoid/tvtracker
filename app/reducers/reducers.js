export var tvShowsReducer = (state = [], action) = {
	switch (action.type) {
		case '':
		default:
			return state;
	}
};

export var showSelectedReducer = (state = undefined, action) = {
	switch (action.type) {
		case '':
		default:
			return state;
	}
};

export var newShowsInfoReducer = (state = {}, action) = {
	switch (action.type) {
		case '':
		default:
			return state;
	}
};

export var addingNewShowReducer = (state = false, action) = {
	switch (action.type) {
		case '':
		default:
			return state;
	}
};
